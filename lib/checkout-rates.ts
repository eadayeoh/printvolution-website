// Shared checkout rates — imported by both the server action that
// computes the order total and the client form that previews it.
// Keeping them here prevents the two from drifting.

export const DELIVERY_FLAT_CENTS = 800;
export const GIFT_WRAP_FLAT_CENTS = 300;
